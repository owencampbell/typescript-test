
export class Fibonacci {

    /*
    Write a function which takes a positive integer, N, as argument and returns N-th element of Fibonacci sequence. Use the following definition of the Fibonacci sequence.
    
    fib(1) = 1
    fib(2) = 1
    fib(n) = fib(n-1) + fib(n-2)
    */

    getValue(n: number): number {
        return 0;
    }

}